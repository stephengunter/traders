using ApplicationCore.Models;
using System;
using System.Collections.Generic;
using System.Text;

namespace ApplicationCore.Indicators
{
    public class BlueChips : Indicator
    {
       
        public override void Calculate()
        {
            //foreach (var quote in quotes)
            //{
            //    var data = quote.DataList.Where(d => d.Indicator == Entity).FirstOrDefault();
            //    if (IsDataInTime(data))
            //    {
            //        let sum = this.getSumVal(i);
            //        item.result = sum;
            //        item.avg = this.countAvg(i);
            //        item.signal = this.createSignal(i);
            //    }
            //    else
            //    {
            //        item.result = 0;
            //        item.avg = 0;
            //        item.signal = 0;
            //    }
            //}

        }
    }
}
